# Lazy imports to avoid requiring all dependencies for every dataset
# Import only what you need:
#   from datasets import sepsis
#   from datasets import speech_commands
#   from datasets import uea
#   from datasets import decompensation
